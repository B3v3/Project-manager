<%= form_for @project.tasks.build, url: project_tasks_path(@project) do |f| %>
  <%= hidden_field_tag :project_id, @project.id %>
  <%= f.label :description %><br />
  <%= f.text_area :description, size: "50x1" %><br />
  <%= f.submit %>
<% end %>
